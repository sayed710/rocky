{
  "name": "@chess-platform/gateway-service",
  "version": "0.1.0",
  "private": true,
  "description": "Deployable realtime gateway service: WebSocket server wrapping RealtimeGateway with shared-secret token verification, durable event log, Redis pub/sub for multi-node fanout, and single-owner command routing (ADR-0010).",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/serve.js",
    "test": "tsc -p tsconfig.test.json && node ../../scripts/run-zero-skip.mjs -- node --test \"dist-test/test/**/*.test.js\"",
    "test:trusted-edge": "npm --prefix ../.. run build:server && npm run build && node ../../scripts/run-zero-skip.mjs -- node --test ../../scripts/nginx-trusted-edge-acceptance.mjs",
    "lint": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist dist-test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@chess-platform/achievements": "file:../../packages/achievements",
    "@chess-platform/api": "file:../../packages/api",
    "@chess-platform/core": "file:../../packages/chess-core",
    "@chess-platform/game": "file:../../packages/game",
    "@chess-platform/persistence": "file:../../packages/persistence",
    "@chess-platform/realtime-gateway": "file:../../packages/realtime-gateway",
    "ioredis": "^5.4.0",
    "ws": "^8.21.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8.11.0",
    "typescript": "^5.5.0"
  }
}
